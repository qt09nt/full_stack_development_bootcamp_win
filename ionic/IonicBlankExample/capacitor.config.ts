import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'IonicBlankExample',
  webDir: 'dists',
  server: {
    androidScheme: 'https'
  }
};

export default config;
